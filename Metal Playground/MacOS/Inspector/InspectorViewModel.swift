import Combine

class InspectorViewModel {
    var inspectorController: InspectorController

    init(inspectorController: InspectorController) {
        self.inspectorController = inspectorController
    }
}
